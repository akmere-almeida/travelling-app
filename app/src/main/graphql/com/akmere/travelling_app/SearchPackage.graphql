query searchPackage($searchTerm: [String]!){
    searchPackage(filters: {
        sku: {
            values: $searchTerm
            operator: OR
        }
    }) {
        results {
            sku
            name
            description
        }
    }
}
